# Raw inputs staged from the analysis repo by scripts/sync_source.py.
# Canonical copies live in ../t-cell-dysfunction-2026; not tracked here.
source/

# Build artifacts produced by etl/build_db.py — regenerated at Docker build time.
# data/parquet/ is COMMITTED so CI (etl/prerender.py) can build the static site
# without the git-ignored source/ sibling repo; downloads are regenerated in CI.
data/downloads/

# Static site emitted by etl/prerender.py (regenerated by CI)
site/

# Local Python
__pycache__/
*.pyc
.venv/
venv/

.claude/
GIT_SETUP.md
.DS_Store
